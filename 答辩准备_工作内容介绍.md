# 答辩准备 - 工作内容介绍

> 依据当前 `develop/tjy` 分支的 git 提交记录整理。重点突出：智能门禁上位机、ZXBee 通信链路、sensor-a/b/c 节点适配、报告与演示材料。

## 一、1-2 分钟自我介绍稿

各位老师好，我是 XX，在本项目中主要负责**智能门禁访客管理系统的上位机应用、通信协议适配、三个传感器节点的数据字段对接，以及最终报告和演示材料整理**。

我的工作可以概括为五个部分：

第一，**上位机系统开发**。我完成了 PyQt5 桌面端应用，包括门禁状态仪表盘、环境数据面板、远程控制面板、访客事件时间线、统计图表和系统状态监控等页面，实现了数据实时展示、控制命令下发、本地 SQLite 存储和 CSV 导出。

第二，**通信链路打通**。我适配了 ZXBee/ZigBee 通信格式，完善了帧解析、字段映射、WebSocket 接入智云平台、串口直连备用方案，使硬件节点、协调器、云平台和上位机能够用统一字段进行交互。

第三，**硬件节点功能适配**。我分别对 sensor-a、sensor-b、sensor-c 做了门禁项目化改造：sensor-a 负责温湿度和光照采集，sensor-b 负责开门、蜂鸣器、RGB 灯和告警复位，sensor-c 负责 PIR、门磁、门铃触摸和夜间安防检测。

第四，**界面和演示功能收敛**。我根据底层实际实现情况，把没有完整闭环的布防/撤防、语音播报等功能从上位机页面隐藏，保证答辩演示时页面展示的功能都能在对应节点中找到实现。

第五，**文档和报告交付**。我生成并维护了项目功能说明、测试命令手册、UI 美化方案、期末报告 Word 文档、架构图、接线图和渲染页面，便于答辩展示和提交。

总结来说，我完成的是从**硬件数据上报、通信协议、上位机展示、远程控制到报告交付**的一条完整链路。

---

## 二、从 git 提交看我做了什么

| 提交 | 主题 | 可以在答辩中怎么讲 |
|---|---|---|
| `165cc87` | 重构 sensor-a 为门禁环境采集节点 | 把原始传感器程序改造成温湿度、光照采集节点，统一上报 `temp/humi/lux` 字段。 |
| `3d8c844` | 升级 sensor-b 控制模块 | 实现门锁继电器、蜂鸣器、RGB 灯、告警等级、自动关门等控制执行逻辑。 |
| `73f6e28` | sensor-c 安防检测节点完整交付 | 集成 PIR、门磁、震动/触摸、安防逻辑、GPIO 中断和测试文档。 |
| `e29f9e2` | 添加协调器 ZXBee 帧解析和构建 | 让协调器能解析/构建统一帧格式，支撑上行数据和下行命令。 |
| `a1ca577` | 修复协调器命令解析问题 | 修正协调器解析细节，保证控制命令能被正确转发。 |
| `a9efd12` | 修复 sensor-c 编译链接和协议问题 | 处理 sensor-c 工程配置、GPIO 头文件和协议接口问题。 |
| `8f89384` | 修复传感器配置和通信协议问题 | 修复 ZXBee 公共协议、接口头文件、sensor-c 上报和配置问题。 |
| `a744fd0` | 完成智能门禁上位机功能 | 完成 PyQt 上位机主体，包括 WebSocket、数据库、多个 UI 面板、系统状态和功能说明。 |
| `3de4649` | 升级 sensor-c 页面支持完整字段显示 | 在上位机中补全 PIR、门磁、门铃、气体、光栅、告警等字段展示，并美化界面。 |
| `034e92d` | 生成期末项目报告 | 生成 Word 报告、架构图、接线图、页面渲染图和报告素材。 |
| `fbafddf` | 优化远程控制面板并更新文档 | 隐藏未闭环功能，修正文档描述，使页面功能和底层实现一致。 |
| `855d3c5` | 重构 RGB LED 控制代码 | 修复 RGB 高低电平、引脚初始化和宏定义冲突问题，增强红绿蓝颜色控制可靠性。 |

这组提交能说明项目不是单点开发，而是按“节点 -> 协议 -> 上位机 -> 文档 -> 修复完善”的顺序推进。

---

## 三、模块工作内容

### 3.1 上位机应用 sensor-e

**相关提交**：`a744fd0`、`3de4649`、`fbafddf`

**主要文件**：

- `sensor-e/main.py`
- `sensor-e/config.py`
- `sensor-e/communication/websocket_client.py`
- `sensor-e/communication/serial_client.py`
- `sensor-e/communication/cloud_api.py`
- `sensor-e/data/database.py`
- `sensor-e/data/models.py`
- `sensor-e/ui/main_window.py`
- `sensor-e/ui/door_status_panel.py`
- `sensor-e/ui/env_info_panel.py`
- `sensor-e/ui/remote_control_panel.py`
- `sensor-e/ui/event_timeline_panel.py`
- `sensor-e/ui/stats_panel.py`
- `sensor-e/ui/system_status_panel.py`
- `sensor-e/resources/style.qss`

**完成内容**：

- 实现 PyQt5 主窗口和多面板布局。
- 通过 WebSocket 接入智云平台，接收硬件节点实时上报数据。
- 提供串口直连备用通道，便于本地调试 ZXBee 帧。
- 实时展示门状态、告警等级、温湿度、光照、PIR、门铃、门磁等字段。
- 远程控制 sensor-b：开门、关门、蜂鸣器短响/关闭、声光告警、复位。
- 远程控制 sensor-c：夜间模式/白天模式切换。
- 记录访客事件时间线。
- 使用 SQLite 保存门禁事件和环境数据。
- 支持 CSV 导出，便于提交和测试留痕。
- 使用 matplotlib 绘制访客统计和事件类型分布。
- 使用 QSS 完成界面美化。
- 根据实际硬件实现隐藏未闭环功能，避免答辩演示时出现“页面有但底层没有”的问题。

**答辩重点说法**：

> 上位机不是单纯显示页面，而是负责把硬件上报数据结构化展示，同时把用户操作转换成统一控制命令下发给对应节点，并把关键事件落库，形成完整的门禁管理闭环。

---

### 3.2 通信协议和协调器

**相关提交**：`e29f9e2`、`a1ca577`、`a9efd12`、`8f89384`、`a744fd0`

**主要文件**：

- `common/zxbee.c`
- `common/zxbee.h`
- `common/zxbee-inf.c`
- `common/zxbee-inf.h`
- `Coordinator/Source/Coordinator.c`
- `sensor-e/communication/websocket_client.py`
- `sensor-e/communication/serial_client.py`

**完成内容**：

- 定义并适配 ZXBee 帧格式：

```text
SOF(0xAA) + DST(2B) + SRC(2B) + CMD(1B) + LEN(1B) + Payload + CHK(1B) + EOF(0x55)
```

- 支持上报、告警、写入、复位等命令类型。
- 完成协调器帧解析和转发逻辑。
- 兼容 JSON 格式和 `{key=value}` 形式的控制数据。
- 修复数组字段解析问题，使 `rgb=[0,255,0]` 不会被逗号错误拆分。
- 上位机 WebSocket 控制命令会转换成节点可解析的数据格式。
- 串口模式下可直接发送 JSON Payload，便于本地测试。

**答辩重点说法**：

> 我把上位机、智云平台、协调器和传感器节点之间的数据格式统一起来，上行和下行都走同一套字段规范，这样后续扩展新传感器时只需要增加字段和页面展示，不需要重写整条通信链路。

---

### 3.3 sensor-a 环境采集节点

**相关提交**：`165cc87`

**主要文件**：

- `sensor-a/Source/sensor.c`

**完成内容**：

| 字段 | 含义 | 说明 |
|---|---|---|
| `temp` | 温度 | HTU21D 温度数据 |
| `humi` | 湿度 | HTU21D 湿度数据 |
| `lux` | 光照 | BH1750 光照数据 |

**节点定位**：

- sensor-a 只负责环境数据采集和上报。
- 不承担开门、蜂鸣器、RGB、安防控制等执行功能。
- 上位机页面中只展示 sensor-a 的环境数据，不放控制按钮。

**典型上报数据**：

```json
{"temp":27.0,"humi":65.0,"lux":200}
```

---

### 3.4 sensor-b 控制执行节点

**相关提交**：`3d8c844`、`855d3c5`

**主要文件**：

- `sensor-b/Source/sensor.c`
- `sensor-b/Source/sensor.h`
- `sensor-b/Source/rgb.c`
- `sensor-b/Source/rgb.h`

**完成内容**：

| 命令字段 | 功能 | 当前实现情况 |
|---|---|---|
| `unlock` | 远程开门 | 已实现，控制继电器吸合，并支持自动回弹关门。 |
| `buzz` | 蜂鸣器 | 已实现，支持短响、长响、关闭。 |
| `rgb` | RGB 灯颜色 | 已实现，支持红/绿/蓝三通道开关量控制。 |
| `alert` | 告警等级 | 已实现，按安全/注意/报警切换灯光和蜂鸣器状态。 |
| `reset` | 告警复位 | 已实现，清除告警状态，关闭蜂鸣器并恢复安全状态。 |

**RGB 修复说明**：

- 原先 RGB 使用通用 `ON/OFF` 宏，容易和 LED、继电器的低电平有效逻辑冲突。
- 已改为 RGB 专用 `RGB_ON/RGB_OFF`。
- 已补全 P0.0、P0.1、P0.2 三个 RGB 引脚的普通 IO 初始化。
- 已修正 `rgb_on/rgb_off` 使用通道编号 `RGB_CH_R/G/B`。
- 当前 RGB 是三路开关量，不是 PWM 调光；`[255,0,0]` 表示红灯亮，不表示亮度 255。

**典型下行命令**：

```json
{"unlock":1,"rgb":[0,255,0]}
{"buzz":500,"rgb":[0,0,255]}
{"alert":2}
{"reset":1}
```

**答辩重点说法**：

> sensor-b 是执行节点，负责把上位机的控制命令转换成硬件动作，比如继电器开门、蜂鸣器提示、RGB 灯状态和告警复位。

---

### 3.5 sensor-c 安防检测节点

**相关提交**：`73f6e28`、`a9efd12`、`8f89384`、`3de4649`

**主要文件**：

- `sensor-c/Source/sensor.c`
- `sensor-c/Source/security_logic.c`
- `sensor-c/Source/security_logic.h`
- `sensor-c/Source/gpio_int.c`
- `sensor-c/Source/gpio_int.h`
- `sensor-c/Source/infrared.c`
- `sensor-c/Source/infrared.h`
- `sensor-c/Source/hall.c`
- `sensor-c/Source/hall.h`
- `sensor-c/Source/Touch.c`
- `sensor-c/Source/Touch.h`
- `sensor-c/Source/vibration.c`
- `sensor-c/Source/vibration.h`

**完成内容**：

| 字段 | 含义 | 说明 |
|---|---|---|
| `pir` | 人体红外 | 检测是否有人靠近。 |
| `door` | 门磁/门状态 | 判断门是否打开。 |
| `tch` | 触摸/门铃 | 用于访客按门铃事件。 |
| `alert` | 告警等级 | 0 安全，1 注意，2 报警。 |
| `night` | 夜间模式 | 控制是否启用夜间入侵判断。 |
| `gas` | 气体字段 | 页面预留和状态展示字段。 |
| `grating` | 光栅字段 | 页面预留和状态展示字段。 |

**安防逻辑**：

- PIR 检测到人员靠近时，上报访客靠近事件。
- 触摸/门铃触发时，上报门铃事件。
- 夜间模式下门被打开，可触发入侵报警。
- 告警等级会同步到上位机页面，并可联动 sensor-b 的声光告警。
- `reset` 用于清除安防告警。

**典型上报数据**：

```json
{"pir":1,"door":0,"tch":0,"alert":0}
{"pir":1,"door":0,"tch":1,"alert":0}
{"pir":1,"door":1,"night":1,"alert":2}
```

---

### 3.6 文档、报告和演示材料

**相关提交**：`034e92d`、`fbafddf`

**主要文件和目录**：

- `build_final_report.py`
- `期末综合项目_智能门禁访客管理系统_完成版.doc`
- `期末综合项目模板.doc`
- `render_final/`
- `report_assets/`
- `screenshots/`
- `项目10_智能门禁访客管理系统_功能说明.md`
- `智云平台测试命令手册.md`
- `QT页面美化方案.md`

**完成内容**：

- 编写报告生成脚本。
- 生成最终 Word 报告。
- 生成系统三层架构图、接线图、页面截图和报告渲染图。
- 整理测试命令，便于现场验证硬件上报和控制下发。
- 更新文档措辞，使报告、页面和底层实现保持一致。

---

## 四、上位机页面和底层实现是否对应

| 上位机页面功能 | 目标节点 | 底层是否实现 | 说明 |
|---|---|---|---|
| 温度显示 | sensor-a | 是 | `temp` 字段上报。 |
| 湿度显示 | sensor-a | 是 | `humi` 字段上报。 |
| 光照显示 | sensor-a | 是 | `lux` 字段上报。 |
| 门状态显示 | sensor-c | 是 | `door` 字段上报。 |
| PIR 人体检测 | sensor-c | 是 | `pir` 字段上报。 |
| 门铃/触摸事件 | sensor-c | 是 | `tch` 字段上报。 |
| 告警等级显示 | sensor-c / sensor-b | 是 | `alert` 字段和声光告警联动。 |
| 远程开门 | sensor-b | 是 | `unlock=1` 控制继电器。 |
| 手动关门/复位门锁 | sensor-b | 是 | 通过 `unlock=0` 或复位逻辑关闭继电器。 |
| 蜂鸣器短响/关闭 | sensor-b | 是 | `buzz` 命令控制。 |
| RGB 颜色变化 | sensor-b | 是 | `rgb=[r,g,b]` 控制三通道。 |
| 声光告警 | sensor-b | 是 | `alert=2` 联动红灯和蜂鸣器。 |
| 告警复位 | sensor-b / sensor-c | 是 | `reset=1` 清除状态。 |
| 夜间/白天模式 | sensor-c | 是 | `night=1/0` 切换模式。 |
| 布防/撤防 | - | 页面已隐藏 | 底层未形成完整闭环，不作为演示功能。 |
| 语音播报 | - | 页面已隐藏 | 依赖额外硬件或未完整实现，不作为演示功能。 |

结论：**当前上位机展示的主要控制功能，都能在 sensor-b 或 sensor-c 中找到对应实现；未完整闭环的功能已经从页面中隐藏。**

---

## 五、答辩时建议强调的亮点

1. **完整闭环**：不是只做页面，而是完成“采集 -> 通信 -> 展示 -> 控制 -> 存储 -> 统计”的闭环。
2. **模块分工清晰**：sensor-a 采环境，sensor-b 做执行，sensor-c 做安防，上位机做管理。
3. **协议统一**：硬件节点和上位机统一使用 `temp/humi/lux/pir/door/tch/alert/rgb/unlock/buzz` 等业务字段。
4. **可演示性强**：页面功能已经和底层实现对齐，答辩时可以按按钮演示。
5. **工程化处理**：有数据库、导出、日志、重连、状态面板、测试命令和报告生成脚本。
6. **问题修复能力**：从 git 提交可以看到多次修复编译、协议、协调器解析、UI 展示和 RGB 控制问题。

---

## 六、常见追问及回答

### Q1：你主要负责哪部分？

> 我主要负责上位机应用和通信适配，同时参与了 sensor-a、sensor-b、sensor-c 三个节点的项目化改造。具体包括 PyQt 页面、WebSocket/串口通信、SQLite 数据存储、统计图表、ZXBee 字段适配、控制命令下发，以及最终报告和演示材料整理。

### Q2：上位机和硬件怎么通信？

> 上行数据由传感器节点通过 ZigBee 发到协调器，再经过智云平台推送到上位机；下行控制由上位机通过 WebSocket 发给智云平台，再经协调器转发到目标节点。串口直连作为备用调试方式。数据格式统一成业务字段，比如 `temp`、`door`、`unlock`、`rgb`。

### Q3：三个节点分别做什么？

> sensor-a 负责环境采集，主要是温湿度和光照；sensor-b 负责执行控制，包括门锁、蜂鸣器、RGB 灯和告警复位；sensor-c 负责安防检测，包括 PIR、门磁、门铃触摸和夜间报警判断。

### Q4：为什么把布防/语音功能隐藏？

> 因为答辩演示要保证页面上的功能都有底层闭环。布防/撤防和语音播报依赖的底层逻辑或额外硬件不完整，所以我把它们从远程控制页面隐藏，保留已经实现并可演示的功能。

### Q5：RGB 之前只亮一种颜色，怎么处理？

> 我检查后发现 RGB 底层存在宏定义和电平逻辑问题。原来 RGB 复用了通用 `ON/OFF`，容易和继电器、普通 LED 的低电平有效逻辑冲突；另外 P0.1、P0.2 初始化也不完整。现在改成 `RGB_ON/RGB_OFF` 专用宏，并补全 P0.0-P0.2 初始化。当前支持红、绿、蓝三路开关量控制。

### Q6：数据有没有保存？

> 有。上位机使用 SQLite 保存门禁事件和环境数据，同时支持 CSV 导出。这样既能现场展示事件时间线，也能导出数据作为测试记录。

### Q7：如果新增一个传感器，怎么扩展？

> 需要在节点端增加驱动和字段上报，在 ZXBee 协议层增加字段映射，在上位机 `config.py` 和数据模型中增加字段，再在对应 UI 面板中展示。因为现在字段和通信链路已经统一，所以扩展成本比较低。

---

## 七、现场演示建议顺序

1. 启动上位机，展示主界面整体布局。
2. 展示环境面板：温度、湿度、光照。
3. 展示门禁状态面板：门状态、告警等级、PIR/门铃状态。
4. 演示远程开门：点击开门，观察继电器和绿色 RGB。
5. 演示门铃或蜂鸣器：触发短响和蓝色提示。
6. 演示声光告警：触发红灯和蜂鸣器。
7. 演示告警复位：恢复安全状态。
8. 展示事件时间线：说明访客记录如何保存。
9. 展示统计图表和 CSV 导出。
10. 最后打开报告/架构图，说明系统结构和模块分工。

---

## 八、答辩时的稳妥表述

- 可以说：**我完成了上位机主体功能，并打通了传感器节点、协调器、智云平台到上位机的主要通信链路。**
- 可以说：**当前演示页面保留的是已经和底层节点对应的功能。**
- 可以说：**RGB 是三通道开关量控制，不是 PWM 亮度调节。**
- 不建议说：**所有扩展字段和所有硬件外设都完整闭环。**
- 不建议说：**语音播报、布防撤防已经完整实现。**

这一版答辩稿的核心思路是：**把你的工作讲成一条完整工程链路，同时避免把未闭环功能讲得过满。**
