# 答辩准备 - 工作内容介绍

## 一、自我介绍话术（1-2分钟）

各位老师好，我是XX，负责智能门禁访客管理系统的**通信协议、全部传感器节点适配和上位机应用**开发。

我的工作主要分五块：

**第一，通信协议层**。我实现了ZXBee帧协议的核心函数，包括帧构建、解析、校验，定义了REPORT/ALARM/WRITE/RESET四种命令字，确保硬件节点、协调器、智云平台和Python应用之间能按统一格式通信。

**第二，环境采集节点sensor-a**。我适配了环境采集节点，将原始的A0/A1/A2字段映射为项目统一的temp/humi/lux字段，实现温湿度和光照数据的定时上报。

**第三，控制执行节点sensor-b**。我适配了控制执行节点，支持unlock（远程开门）、buzz（蜂鸣器）、rgb（灯光控制）、reset（告警复位）等下行控制命令的解析和执行。

**第四，安防检测节点sensor-c**。我适配了安防节点，将A0/A1/A2/A3字段映射为pir/tch/door/alert字段，完成PIR、门磁、门铃/触摸等安防状态上报，并支持夜间模式下的告警判断。

**第五，PyQt5上位机应用，这是我主要的工作**。我从零搭建了整个桌面应用，包括：
- WebSocket实时通信模块，实现与智云平台的双向数据交互
- 6个UI界面：门状态仪表盘、环境信息面板、远程控制面板、访客事件时间线、统计图表、系统状态监控
- SQLite本地数据存储和CSV导出
- 使用QSS样式表进行UI美化

总共编写了**3600多行代码**，完成了项目**70%以上**的功能。

以上是我的工作介绍，请老师提问。

---

## 二、各模块详细工作内容

### 2.1 通信协议层

**改动文件**：
- `common/zxbee.c` / `zxbee.h` - ZXBee协议核心实现
- `common/zxbee-inf.c` / `zxbee-inf.h` - ZigBee接口层

**完成内容**：
- 帧构建/解析/校验函数（ZXBee_BuildFrame/ZXBee_ParseFrame/ZXBee_CheckSum）
- JSON负载编解码（ZXBeeDecodeJsonPayload/ZXBeeDecodeLegacyPayload）
- 系统命令和业务命令分发（ZXBeeSysCommandProc/ZXBeeUserProcess）
- 命令字定义：CMD_REPORT(0x01)、CMD_ALARM(0x02)、CMD_WRITE(0x03)、CMD_RESET(0x06)

**帧格式**：
```
SOF(0xAA) + DST(2B) + SRC(2B) + CMD(1B) + LEN(1B) + JSON Payload + CHK(1B) + EOF(0x55)
```

---

### 2.2 环境采集节点sensor-a

**改动文件**：
- `sensor-a/Source/sensor.c` - 环境采集节点主程序

**完成内容**：
- 节点地址：SRC=0x01，节点名=601
- 字段映射：
  | 原字段 | 项目字段 | 含义 |
  |--------|----------|------|
  | A0 | temp | 温度（HTU21D） |
  | A1 | humi | 湿度（HTU21D） |
  | A2 | lux | 光照（BH1750） |
- 定时上报：每30秒（V0可配置）上报一次环境数据
- 查询支持：支持`?`查询当前温湿度和光照值

**典型上报数据**：
```json
{"temp":27.0,"humi":65.0,"lux":200}
```

---

### 2.3 控制执行节点sensor-b

**改动文件**：
- `sensor-b/Source/sensor.c` - 控制执行节点主程序

**完成内容**：
- 节点地址：SRC=0x02，节点名=602
- 支持的下行控制命令：
  | 字段 | 含义 | 处理方式 |
  |------|------|----------|
  | unlock | 远程开门 | 映射继电器1，吸合3秒后自动关闭 |
  | buzz | 蜂鸣器 | 映射蜂鸣器控制位（500ms短鸣/长鸣/停止） |
  | rgb | RGB灯 | 解析`[r,g,b]`数组，映射RGB控制位 |
  | reset | 告警复位 | 清空控制状态，关闭继电器 |
  | alert | 告警等级 | 设置声光告警等级 |

**典型下行数据**：
```json
{"unlock":1,"rgb":[0,255,0]}    // 开门+绿灯
{"buzz":500,"rgb":[0,0,255]}    // 蜂鸣器短鸣+蓝灯
{"reset":1}                     // 复位告警
```

---

### 2.4 安防检测节点sensor-c

**改动文件**：
- `sensor-c/Source/sensor.c` - 安防检测节点主程序
- `sensor-c/Source/gpio_int.h` - GPIO中断配置
- `sensor-c/Source/infrared.h` - PIR驱动接口

**完成内容**：
- 节点地址：SRC=0x03，节点名=603
- 字段映射：
  | 原字段 | 项目字段 | 含义 |
  |--------|----------|------|
  | A0 | pir | 人体红外（HC-SR501） |
  | A1 | tch | 震动/触摸/门铃 |
  | A2 | door | 门磁/门状态 |
  | A3 | alert | 告警等级（0/1/2） |
  | A4 | gas | 可燃气体 |
  | A5 | grating | 红外光栅 |
- GPIO中断模式：PIR/震动/门磁/触摸均支持中断触发，事件上报延迟<100ms
- 下行控制：支持`night=1/0`切换夜间/白天模式，支持`reset=1`清除安防告警
- 安防逻辑集成：
  - 徘徊检测：PIR持续>10秒且未按门铃 → alert=1（注意）
  - 夜间入侵：夜间模式+门被打开 → alert=2（报警）
  - 三级告警：0=安全(绿)、1=警戒(蓝)、2=报警(红+蜂鸣器)

**典型上报数据**：
```json
{"pir":1,"door":0,"tch":0,"alert":0}    // 有人靠近，门关着，无告警
{"pir":1,"door":0,"tch":1,"alert":0}    // 有人按门铃
{"pir":1,"door":1,"alert":2,"night":1}  // 夜间入侵告警
```

---

### 2.5 PyQt5上位机应用（主要工作）

**改动文件**（15个）：

#### 2.5.1 通信模块
- `communication/websocket_client.py` - WebSocket实时通信（222行）
  - 连接智云平台，接收硬件上报数据
  - 发送控制命令到硬件
  - 自动重连机制和心跳检测
- `communication/cloud_api.py` - 智云平台HTTP API
- `communication/serial_client.py` - 串口直连备用方案

#### 2.5.2 UI界面（6个面板）
- `ui/main_window.py` - 主窗口框架（QWidget+QDockWidget布局）
- `ui/door_status_panel.py` - 门状态仪表盘（LCD风格显示）
- `ui/env_info_panel.py` - 环境信息面板（温湿度/光照）
- `ui/remote_control_panel.py` - 远程控制面板（开门/蜂鸣器/报警/夜间模式）
- `ui/event_timeline_panel.py` - 访客事件时间线（QTreeWidget）
- `ui/stats_panel.py` - 统计图表（matplotlib柱状图/饼图）
- `ui/system_status_panel.py` - 系统状态监控

#### 2.5.3 数据层
- `data/database.py` - SQLite数据库操作（access_log表+door_env表）
- `data/models.py` - 数据模型定义

#### 2.5.4 资源和配置
- `config.py` - 系统配置（API地址、字段定义）
- `resources/style.qss` - QT样式表（273行，UI美化）
- `main.py` - 应用入口

**上位机当前页面展示功能**：
- 实时显示门状态、环境数据
- 远程控制：开门、关门、蜂鸣器、RGB灯、报警触发/复位、夜间/白天模式切换
- 访客事件时间线记录
- 统计图表：日/周访客统计柱状图、事件类型分布饼图
- 数据导出：CSV格式导出访问日志和环境数据

**页面不展示的未闭环功能**：
- sensor-a不展示控制按钮：sensor-a定位为环境采集节点，只负责温湿度和光照上报/查询
- 布防/撤防、语音播报不作为演示功能：底层实现不完整或依赖额外硬件条件，已从远程控制面板隐藏

---

### 2.6 文档编写（6个）

| 文档名称 | 内容说明 |
|----------|----------|
| `项目10_智能门禁访客管理系统_功能说明.md` | 详细功能规格说明，包含所有模块的字段定义和接口规范 |
| `智云平台测试命令手册.md` | 测试命令和调试方法，用于验证通信链路 |
| `QT页面美化方案.md` | UI设计方案（698行），包含配色、布局、样式规范 |
| `D部分通信协议与ZigBee完成说明.md` | 协议层完成报告，记录帧格式和字段映射 |
| `通信接口说明.md` | 接口字段定义，各节点的上行/下行数据格式 |
| `接线说明_安防传感器节点.md` | sensor-c硬件接线指南 |

---

### 2.7 期末报告生成

- `build_final_report.py` - 报告生成脚本（421行）
- `render_final/` - 12张渲染好的报告页面
- `report_assets/` - 架构图和接线图
- `期末综合项目_智能门禁访客管理系统_完成版.doc` - 最终提交文档

---

## 三、工作量统计

| 模块 | 文件数 | 代码行数 | 占比 |
|------|--------|----------|------|
| 通信协议层 | 4个 | ~500行 | 14% |
| sensor-a环境节点 | 1个 | ~100行 | 3% |
| sensor-b控制节点 | 1个 | ~150行 | 4% |
| sensor-c安防节点 | 3个 | ~200行 | 6% |
| **PyQt5上位机** | **15个** | **~2500行** | **70%** |
| 报告生成脚本 | 1个 | 421行 | 3% |
| **总计代码** | **25个** | **~3871行** | **100%** |
| 文档 | 6个 | ~2000行 | - |

**结论**：完成项目**70%以上**的功能，代码量**3800+行**。

---

## 四、常见追问及回答

### Q1: 你负责哪部分？
> "我负责通信协议、三个传感器节点适配和上位机应用，完成了项目70%以上的功能。"

### Q2: 上位机怎么和硬件通信的？
> "通过智云平台中转。上位机用WebSocket连接智云平台，硬件通过协调器的串口连接平台。上行数据：传感器→ZigBee→协调器→串口→智云→WebSocket→上位机。下行控制：上位机→WebSocket→智云→串口→协调器→ZigBee→传感器节点。"

### Q3: 三个节点分别做什么？
> "sensor-a负责环境采集（温湿度/光照），sensor-b负责控制执行（开门/蜂鸣器/灯/告警复位），sensor-c负责安防检测（人体红外/门磁/门铃/夜间模式）。三个节点通过统一的ZXBee协议按职责上报数据或接收控制命令。"

### Q4: 安防逻辑怎么实现的？
> "在sensor-c中实现了三级告警：PIR持续超过10秒未按门铃是徘徊检测（警戒级别1），夜间模式下门被打开是夜间入侵（报警级别2）。通过security_logic模块实现状态机判断。"

### Q5: UI怎么设计的？
> "采用QWidget主窗口+QDockWidget停靠面板的布局，左侧是门状态和环境信息，中间是事件时间线，右侧是远程控制和统计图表。使用QSS样式表统一视觉风格。"

### Q6: 遇到什么问题？
> "主要有三个：
> 1. 编码问题 - 源文件是GBK，git提交后乱码，通过.gitattributes配置解决
> 2. 字段不统一 - 原始代码用A0/A1，上位机期望pir/tch，在ZXBeeUserProcess中做了兼容处理
> 3. WebSocket断线 - 增加了自动重连机制和心跳检测"

### Q7: 为什么用WebSocket不用HTTP？
> "因为门禁系统需要实时性。HTTP是请求-响应模式，上位机无法主动收到硬件事件。WebSocket是全双工通信，硬件有事件时智云平台能立即推送给上位机，延迟在秒级以内。"

### Q8: 如果要增加新传感器，需要改哪些地方？
> "1. 新增传感器驱动文件
> 2. 在sensor.c中添加update函数和ZXBeeAdd调用
> 3. 在ZXBeeUserProcess中添加查询支持
> 4. 在上位机config.py中添加字段定义
> 5. 在数据库中添加对应列"

---

## 五、答辩注意事项

1. **语速适中**：不要太快，让老师听清楚
2. **眼神交流**：不要一直看屏幕或稿子
3. **主动引导**：提到"上位机是我主要工作"时可以打开演示
4. **诚实回答**：不会的说"这部分我了解不深"，不要硬编
5. **控制时间**：自我介绍控制在1-2分钟内
6. **准备演示**：提前测试上位机能否正常运行

---

## 六、演示脚本（可选）

如果需要现场演示上位机，按以下顺序：

1. **启动应用**：运行main.py，展示主界面
2. **门状态面板**：展示门的开关状态、告警级别
3. **环境面板**：展示温湿度和光照数据
4. **远程控制**：演示开门、关门、蜂鸣器、告警触发/复位、夜间模式切换
5. **事件时间线**：展示访客记录
6. **统计图表**：展示柱状图和饼图
7. **数据导出**：演示CSV导出功能

每个功能配合一句话说明，控制在5分钟内完成演示。
