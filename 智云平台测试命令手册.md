# 智云平台测试命令手册

## 一、项目整体逻辑架构

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              智云平台 (api.zhiyun360.com)                         │
│                         WebSocket / HTTP / TCP 通信中心                           │
└─────────────────────────────────────────────────────────────────────────────────
                                      │
                    ┌─────────────────┼─────────────────┐
                    │                 │                 │
                    ▼                 ▼                 ▼
            ┌──────────────┐  ┌──────────────┐  ┌──────────────┐
            │   sensor-a   │  │   sensor-b   │  │   sensor-c   │
            │  环境采集节点  │  │  控制执行节点  │  │  安防检测节点  │
            │    (601)      │  │    (602)      │  │    (603)      │
            └──────────────┘  └──────────────┘  └──────────────┘
                    │                 │                 │
                    └─────────────────┼─────────────────┘
                                      │
                                      ▼
                            ┌──────────────────┐
                            │  Python QT上位机  │
                            │   (sensor-e)     │
                            │  数据可视化中心   │
                            └──────────────────┘
```

### 数据流向说明

1. **上行数据（传感器 → 智云 → QT）**
   - sensor-a: 定时上报温湿度、光照数据
   - sensor-b: 定时上报门锁、蜂鸣器、RGB灯、告警状态
   - sensor-c: 定时/变化上报PIR、门磁、震动、火焰、气体等安防数据

2. **下行命令（QT → 智云 → 传感器）**
   - QT发送控制命令到sensor-b（门锁、蜂鸣器、RGB灯、告警）
   - QT发送控制命令到sensor-c（夜间模式、布防、继电器）

---

## 二、各节点功能详解

### 2.1 sensor-a — 环境采集节点 (601)

**功能**: 采集温湿度、光照数据，定时上报

**上报字段**:
| 字段 | 含义 | 示例值 | 说明 |
|------|------|--------|------|
| temp | 温度 | 27.0 | 单位：摄氏度 |
| humi | 湿度 | 65.0 | 单位：%RH |
| lux | 光照 | 200 | 单位：lux |

**控制命令**:
| 命令 | 格式 | 功能 | 预期反应 |
|------|------|------|----------|
| 查询温度 | `A0=?` 或 `temp=?` | 查询当前温度 | 返回 `{"temp":27.0}` |
| 查询湿度 | `A1=?` 或 `humi=?` | 查询当前湿度 | 返回 `{"humi":65.0}` |
| 查询光照 | `A2=?` 或 `lux=?` | 查询当前光照 | 返回 `{"lux":200}` |
| 查询使能 | `D0=?` | 查询上报使能位 | 返回 `{"D0":255}` |
| 查询周期 | `V0=?` | 查询上报周期 | 返回 `{"V0":30}` |
| 设置周期 | `V0=<秒>` | 设置上报周期 | 返回 `{"V0":<新值>}` |
| 关闭上报 | `CD0=<位掩码>` | 关闭指定传感器上报 | 无直接返回 |
| 开启上报 | `OD0=<位掩码>` | 开启指定传感器上报 | 无直接返回 |

---

### 2.2 sensor-b — 控制执行节点 (602)

**功能**: 控制门锁、蜂鸣器、RGB灯，响应告警

**上报字段**:
| 字段 | 含义 | 示例值 | 说明 |
|------|------|--------|------|
| unlock | 门锁状态 | 0/1 | 0=锁定，1=解锁 |
| buzz | 蜂鸣器状态 | 0/1/2 | 0=关闭，1=长响，2=短响 |
| rgb | RGB灯颜色 | [0,255,0] | [R,G,B] 格式 |
| alert | 告警等级 | 0/1/2 | 0=安全，1=注意，2=报警 |

**控制命令**:
| 命令 | 格式 | 功能 | 预期反应 |
|------|------|------|----------|
| 远程开门 | `unlock=1` | 打开继电器 | 继电器开+绿灯+3秒自动关门 |
| 手动关门 | `unlock=0` | 关闭继电器 | 继电器关 |
| 触发门铃 | `buzz=500` | 门铃模式 | 蜂鸣器响500ms+蓝灯5秒 |
| 关闭蜂鸣器 | `buzz=0` | 静音 | 蜂鸣器关闭+设置静音标志 |
| 蜂鸣器长响 | `buzz=1` | 持续响 | 蜂鸣器持续响 |
| 设置RGB | `rgb=[R,G,B]` | 设置灯颜色 | RGB灯变色 |
| 设置告警 | `alert=0/1/2` | 设置告警等级 | 0=绿灯，1=蓝灯，2=红灯+响 |
| 解除告警 | `reset=1` | 系统复位 | 告警恢复+关蜂鸣器+关门+绿灯 |

---

### 2.3 sensor-c — 安防检测节点 (603)

**功能**: 安防检测，支持两种工作模式

**Mode 1（安防模式）上报字段**:
| 字段 | 含义 | 示例值 | 说明 |
|------|------|--------|------|
| pir | PIR人体红外 | 0/1 | 0=无人，1=有人 |
| tch | 震动/敲门 | 0/1 | 0=无，1=有 |
| door | 霍尔门磁 | 0/1 | 0=关闭，1=打开 |
| flame | 火焰检测 | 0/1 | 0=正常，1=检测到 |
| gas | 可燃气体 | 0/1 | 0=正常，1=超标 |
| grating | 红外光栅 | 0/1 | 0=正常，1=遮断 |
| alert | 告警等级 | 0/1/2 | 由安防逻辑自动计算 |
| stay | PIR持续时间 | 秒数 | PIR持续触发的时间 |
| night | 夜间模式 | 0/1 | 0=白天，1=夜间 |

**Mode 2（门禁/语音模式）上报字段**:
| 字段 | 含义 | 示例值 | 说明 |
|------|------|--------|------|
| touch | 触摸按键/门铃 | 0/1 | 0=未按，1=按下 |
| gas | 可燃气体 | 0/1 | 同Mode 1 |
| grating | 红外光栅 | 0/1 | 同Mode 1 |
| alert | 告警等级 | 0/1/2 | 同Mode 1 |
| stay | 持续时间 | 秒数 | 同Mode 1 |
| night | 夜间模式 | 0/1 | 同Mode 1 |

**控制命令**:
| 命令 | 格式 | 功能 | 预期反应 |
|------|------|------|----------|
| 查询PIR | `pir=?` 或 `A0=?` | 查询PIR状态 | 返回 `{"pir":0/1}` |
| 查询门磁 | `door=?` 或 `A2=?` | 查询门磁状态 | 返回 `{"door":0/1}` |
| 查询震动 | `tch=?` 或 `A1=?` | 查询震动状态 | 返回 `{"tch":0/1}` |
| 设置夜间 | `night=0/1` | 切换夜间模式 | 返回 `{"night":0/1}` |
| 布防模式 | `arm=0/1/2` | 设置布防 | arm=2等效night=1 |
| 解除告警 | `reset=1` | 远程解除告警 | 告警恢复+关继电器 |
| 设置周期 | `V0=<秒>` | 设置上报周期 | 返回 `{"V0":<新值>}` |
| 语音播报 | `V1=<hex>` | 播放语音 | SYN6288播放语音 |
| 继电器控制 | `OD1/CD1=<掩码>` | 控制继电器 | bit6=继电器1，bit7=继电器2 |

---

## 三、QT上位机功能分析

### 3.1 已实现功能

| 功能模块 | 实现状态 | 说明 |
|---------|---------|------|
| 门禁状态看板 | ✅ | 显示PIR、门磁、门铃、告警状态 |
| 环境信息面板 | ✅ | 显示温度、湿度、光照 |
| 远程控制面板 | ✅ | 开门、门铃、告警、夜间模式控制 |
| 事件时间线 | ✅ | 显示历史事件记录 |
| 统计图表 | ✅ | Matplotlib图表展示 |
| 系统状态面板 | ✅ | 显示连接状态、数据统计 |
| 数据库存储 | ✅ | SQLite/MySQL双模式 |
| WebSocket通信 | ✅ | 连接智云平台 |

### 3.2 缺失功能（需要实现）

| 缺失功能 | 优先级 | 说明 |
|---------|--------|------|
| **sensor-c完整数据显示** | 高 | 缺少flame、gas、grating字段显示 |
| **语音播报控制** | 中 | 缺少V1语音播报按钮 |
| **布防模式控制** | 中 | 缺少arm命令控制 |
| **继电器控制** | 中 | 缺少sensor-c的继电器控制 |
| **上报周期调整** | 低 | 缺少V0周期调整界面 |
| **传感器使能控制** | 低 | 缺少D0使能位控制 |
| **实时数据曲线** | 中 | 缺少实时数据折线图 |
| **告警声音提示** | 中 | 缺少告警时的声音提示 |
| **多用户登录** | 低 | 缺少用户权限管理 |

---

## 四、完整测试流程

### 4.1 环境数据采集测试

```
测试目标：验证sensor-a的数据采集和显示

步骤：
1. 启动QT上位机
2. 观察环境信息面板
3. 验证温度、湿度、光照数据是否正常显示
4. 发送查询命令：A0=?、A1=?、A2=?
5. 验证返回值与显示值一致

预期结果：
- 温度显示范围：0-50℃
- 湿度显示范围：0-100%
- 光照显示范围：0-65535 lux
```

### 4.2 门锁控制测试

```
测试目标：验证远程开门功能

步骤：
1. 点击"远程开门"按钮
2. 观察QT状态栏显示"已发送开门命令"
3. 验证sensor-b的继电器状态
4. 等待3秒，验证自动关门

预期结果：
- 发送命令：{"unlock":1,"rgb":[0,255,0]}
- sensor-b继电器打开
- RGB灯变绿
- 3秒后继电器自动关闭
```

### 4.3 门铃测试

```
测试目标：验证门铃触发功能

步骤：
1. 点击"触发门铃"按钮
2. 观察sensor-b的蜂鸣器
3. 观察RGB灯颜色

预期结果：
- 发送命令：{"buzz":500,"rgb":[0,0,255]}
- 蜂鸣器响500ms
- RGB灯变蓝5秒
- 5秒后恢复原颜色
```

### 4.4 告警测试

```
测试目标：验证告警触发和解除

步骤：
1. 点击"触发告警"按钮
2. 观察sensor-b的蜂鸣器和RGB灯
3. 点击"解除告警"按钮
4. 观察状态恢复

预期结果：
- 触发告警：红灯+蜂鸣器长响
- 解除告警：绿灯+蜂鸣器关闭
```

### 4.5 夜间模式测试

```
测试目标：验证夜间模式切换

步骤：
1. 点击"启用夜间模式"按钮
2. 观察sensor-c的night字段
3. 点击"白天模式"按钮
4. 观察状态恢复

预期结果：
- 启用夜间：{"night":1}发送到sensor-c
- sensor-c进入夜间安防模式
- 切换白天：{"night":0}发送到sensor-c
```

### 4.6 安防联动测试

```
测试目标：验证安防事件联动

步骤：
1. 启用夜间模式
2. 在sensor-c门前移动（触发PIR）
3. 打开门（触发门磁）
4. 观察告警联动

预期结果：
- PIR触发：alert=1（注意），蓝灯
- 夜间开门：alert=2（报警），红灯+蜂鸣器
- 事件记录到数据库
- 时间线显示事件
```

---

## 五、常见问题排查

### 5.1 数据不更新

```
检查项：
1. 智云平台连接状态（QT标题栏显示"● 智云已连接"）
2. WebSocket是否正常连接
3. 传感器节点是否在线
4. 上报周期设置是否正确
```

### 5.2 命令发送失败

```
检查项：
1. 网络连接是否正常
2. 智云平台认证是否成功
3. 目标节点是否在线
4. 命令格式是否正确
```

### 5.3 数据库错误

```
检查项：
1. SQLite文件是否存在且有写权限
2. MySQL连接配置是否正确
3. 数据库表是否已创建
```

---

## 六、技术细节

### 6.1 ZXBee协议帧格式

```
[0xAA] [dst_H] [dst_L] [src_H] [src_L] [cmd] [len] [payload...] [checksum] [0x55]

- SOF: 0xAA（帧起始）
- dst: 目标地址（2字节）
- src: 源地址（2字节）
- cmd: 命令类型（0x01=上报，0x02=告警，0x03=控制，0x06=重置）
- len: payload长度
- payload: JSON数据
- checksum: 累加和低8位
- EOF: 0x55（帧结束）
```

### 6.2 节点地址分配

| 节点 | 地址 | 说明 |
|------|------|------|
| 协调器 | 0x0000 | 网络中心 |
| sensor-a | 0x0001 | 环境采集 |
| sensor-b | 0x0002 | 控制执行 |
| sensor-c | 0x0003 | 安防检测 |

### 6.3 告警等级定义

| 等级 | 值 | RGB颜色 | 蜂鸣器 | 触发条件 |
|------|-----|---------|--------|----------|
| 安全 | 0 | 绿色 | 关闭 | 默认状态 |
| 注意 | 1 | 蓝色 | 关闭 | PIR持续>10秒/夜间震动 |
| 报警 | 2 | 红色 | 长响 | 夜间开门/入侵 |

---

## 七、更新日志

- 2026-06-15: 完善测试命令手册，补充所有节点的命令和预期反应
- 2026-06-15: 添加QT上位机功能分析和缺失功能列表
- 2026-06-15: 添加完整测试流程和问题排查指南
