# 项目10 智能门禁访客管理系统功能说明

## 1. 项目要求摘要

根据 `10-项目10：智能门禁访客管理系统.docx` 和总体规范，本项目需要完成一个完整的三层物联网系统：

| 层次 | 要求 | 本项目对应 |
| --- | --- | --- |
| 硬件层 | CC2530 采集/控制，安防类、采集类、控制类传感器至少各一种 | sensor-a 环境采集，sensor-b 控制执行，sensor-c 门禁安防 |
| 传输层 | Z-Stack 组网，ZXBee 帧封包，智云平台 WebSocket/控制下发 | common/zxbee 与 common/zxbee-inf 封包转发，Python 使用智云 WebSocket |
| 应用层 | PyQt5 实时看板、远程控制、事件时间线、统计图、MySQL CRUD | sensor-e Python 上位机 |

核心业务要求：

- PIR 检测访客靠近门口。
- 门磁检测门开/关。
- TCH/触摸作为门铃事件。
- 停留超过 10 秒且未按门铃，记为徘徊事件。
- 夜间模式下门被打开，触发告警。
- HTU21D/BH1750 上报温湿度和光照。
- Python 界面可远程开门、关门、触发门铃、触发/解除告警、切换夜间/白天模式。
- 数据写入 MySQL，并能查询、统计、导出。

## 2. 节点功能实现情况

### sensor-a：环境采集节点

路径：`zstack-2.4.0-1.4.0x/Projects/zstack/Samples/sensor-a/`

已实现：

- 初始化 HTU21D 温湿度传感器。
- 初始化 BH1750 光照传感器。
- 定时上报：

```json
{"temp":29.7,"humi":45.5,"lux":374.2}
```

- 支持查询字段：

| 字段 | 含义 |
| --- | --- |
| `temp` / `A0` | 温度 |
| `humi` / `A1` | 湿度 |
| `lux` / `A2` | 光照 |
| `V0` | 上报周期 |
| `D0` / `CD0` / `OD0` | 上报字段使能 |

逻辑：

1. 入网后立即上报一次环境数据。
2. `MY_REPORT_EVT` 定时触发 `sensorUpdate()`。
3. `sensorUpdate()` 读取温湿光照，构造 JSON，经 `ZXBeeInfSend()` 封装成 ZXBee 帧发往协调器和智云。

状态：已满足项目的采集类传感器要求。

### sensor-b：控制执行节点

路径：`zstack-2.4.0-1.4.0x/Projects/zstack/Samples/sensor-b/`

已实现：

- RGB LED 控制。
- 蜂鸣器控制。
- 继电器模拟门锁。
- 远程开门后 3 秒自动关门。
- 门铃短响 500ms，蓝灯保持约 5 秒。
- 告警状态红灯和蜂鸣器长响。
- 解除告警恢复绿灯安全状态。
- 定时上报当前执行器状态：

```json
{"unlock":0,"buzz":0,"rgb":[0,255,0],"alert":0}
```

支持下发命令：

| 命令 | 目标节点 | 功能 |
| --- | --- | --- |
| `{unlock=1}` | sensor-b | 远程开门，继电器打开，3 秒后自动关闭 |
| `{unlock=0}` | sensor-b | 手动关门/锁定 |
| `{buzz=500}` | sensor-b | 门铃短响 500ms |
| `{buzz=1}` | sensor-b | 蜂鸣器长响 |
| `{buzz=0}` | sensor-b | 关闭蜂鸣器 |
| `{rgb=[0,255,0]}` | sensor-b | 绿灯，安全 |
| `{rgb=[0,0,255]}` | sensor-b | 蓝灯，门铃/注意 |
| `{rgb=[255,0,0]}` | sensor-b | 红灯，告警 |
| `{alert=2}` | sensor-b | 触发声光告警 |
| `{reset=1}` | sensor-b | 解除告警并复位 |

逻辑：

1. `ZXBeeUserProcess()` 解析智云/协调器下发字段。
2. `unlock=1` 调用 `relayUnlock()`，继电器开，3 秒后 `MY_RELAY_EVT` 自动关。
3. `buzz=500` 调用 `doorbellTrigger()`，短响并蓝灯。
4. `alert=2` 切换告警级别，红灯和蜂鸣器长响。
5. `reset=1` 关闭蜂鸣器、清门铃状态、关门锁、恢复绿灯。

状态：已满足项目的控制类执行器要求。

### sensor-c：门禁安防节点

路径：`zstack-2.4.0-1.4.0x/Projects/zstack/Samples/sensor-c/`

已实现：

- PIR 人体红外检测，字段 `pir`。
- 门磁/霍尔检测，字段 `door`。
- 触摸/震动模拟门铃，字段 `tch`。
- 安防状态逻辑：`alert`、`stay`、`night`。
- GPIO 中断触发后立即上报。
- 定时/变化上报。
- 支持夜间模式和复位：

| 命令 | 目标节点 | 功能 |
| --- | --- | --- |
| `{night=1}` | sensor-c | 强制夜间模式 |
| `{night=0}` | sensor-c | 白天/普通模式 |
| `{arm=2}` | sensor-c | 夜间布防兼容命令 |
| `{reset=1}` | sensor-c | 清除安防告警 |
| `{pir=?}` | sensor-c | 查询 PIR |
| `{tch=?}` | sensor-c | 查询门铃 |
| `{door=?}` | sensor-c | 查询门状态 |
| `{alert=?}` | sensor-c | 查询告警状态 |

典型上报：

```json
{"pir":1,"door":1}
{"tch":1,"alert":0,"stay":33,"night":0}
{"pir":1,"door":1,"alert":1,"stay":15,"night":0}
{"door":1,"night":1,"alert":2}
```

逻辑：

1. `sensorCheck()` 周期读取 PIR、门磁、门铃等状态。
2. `security_logic_update()` 计算停留时间、门铃状态、夜间状态和告警等级。
3. PIR 从 0 到 1 记为有人靠近。
4. PIR 持续超过 10 秒且本轮未按门铃，记为徘徊，`alert=1`。
5. 夜间模式下门从关到开，记为入侵，`alert=2`。
6. `reset=1` 调用 `security_logic_reset_alert()` 清除告警。

状态：已满足项目的安防类传感器和联动逻辑要求。

## 3. 公共 ZXBee 协议实现

路径：`zstack-2.4.0-1.4.0x/Projects/zstack/Samples/common/`

已实现：

- `ZXBee_BuildFrame()`：构造 `SOF + DST + SRC + CMD + LEN + PAYLOAD + CHK + EOF`。
- `ZXBee_ParseFrame()`：校验帧头、帧尾、长度、校验和。
- `ZXBeeDecodePackage()`：兼容 ZXBee JSON 帧和旧式 `{A0=...}` 数据。
- `ZXBeeInfSend()`：自动封包并通过 ZigBee 发送。

命令字：

| CMD | 含义 | 使用场景 |
| --- | --- | --- |
| `0x01` | 普通上报 | temp/humi/lux、普通状态 |
| `0x02` | 安防事件 | alert=1/2、stay 字段事件 |
| `0x03` | 写命令 | unlock/buzz/rgb/arm |
| `0x06` | 解除/复位 | reset |

已修改问题：

- 普通 `night=1` 只是夜间模式状态，不应单独被判定为 `0x02` 安防事件；已改为只有 `alert=1/2` 或 `stay` 才判定为安防事件。
- `arm` 字段已归入写命令判断。

## 4. sensor-e Python 上位机实现

路径：`zstack-2.4.0-1.4.0x/Projects/zstack/Samples/sensor-e/`

### 通信

已实现：

- 使用智云 WebSocket：

```text
wss://api.zhiyun360.com:28090
```

- 启动后发送认证：

```json
{"method":"authenticate","uid":"736952991135","key":"...","version":"0.1.0","autodb":true}
```

- 接收智云推送：

```json
{"method":"message","addr":"00:12:4B:00:1C:45:BD:01","data":"..."}
```

- 解析硬件包装帧中的 JSON Payload。
- 根据 MAC 地址识别 `sensor-a`、`sensor-b`、`sensor-c`。
- 支持 `echo` 心跳原样回复。

### PyQt 页面

页面包含：

| 区域 | 显示内容 |
| --- | --- |
| 门禁状态看板 | 访客状态、门状态、门铃状态、安全模式 |
| 环境信息 | 温度、湿度、光照 |
| 远程控制 | 开门、关门、触发门铃、关闭蜂鸣器、触发告警、解除告警、夜间/白天模式 |
| 运行诊断 | 智云状态、各节点收包数、数据库统计、最近解析数据 |
| 访客事件时间线 | doorbell、pir_detect、loiter、door_open、door_close、intrusion、remote_unlock、alert_reset |
| 访客统计 | 近 7 日访客次数、事件类型分布 |

已美化和自适应：

- 主窗口加入滚动容器，窗口高度不够时可滚动。
- 状态卡片、环境卡片、运行诊断卡片改为紧凑网格布局。
- 统计图高度和留白优化，避免单日数据柱状图顶满画布。
- 按钮布局改为多行栅格，缩放时更稳定。

### MySQL 数据库

数据库：`door_access`

表 1：`access_log`

| 字段 | 含义 |
| --- | --- |
| `id` | 主键 |
| `student_id` | 学号/学生标识，可为空 |
| `sensor` | 触发源，如 pir/door/tch/remote |
| `event_type` | 事件类型 |
| `value` | 事件值 |
| `alert` | 0 安全，1 注意，2 报警 |
| `is_remote_unlock` | 是否远程开门 |
| `create_time` | 时间 |

表 2：`door_env`

| 字段 | 含义 |
| --- | --- |
| `id` | 主键 |
| `student_id` | 学号/学生标识，可为空 |
| `temp` | 温度 |
| `humi` | 湿度 |
| `lux` | 光照 |
| `create_time` | 时间 |

已实现 CRUD：

| 操作 | Python 方法 |
| --- | --- |
| INSERT 事件 | `insert_access_log()` |
| SELECT 事件 | `select_access_logs()` |
| UPDATE 远程开门标记 | `update_access_log_unlock()` |
| DELETE 旧事件 | `delete_old_access_logs()` |
| INSERT 环境 | `insert_door_env()` |
| SELECT 环境 | `select_door_env()` |
| CSV 导出 | `export_table_csv()` |

## 5. PyQt 按钮与下发命令

| 页面按钮 | 下发目标 | WebSocket control data | 预期硬件现象 | 页面显示 |
| --- | --- | --- | --- | --- |
| 远程开门 | sensor-b | `{unlock=1,rgb=[0,255,0]}` | 继电器打开，绿灯，3 秒后自动关 | 事件时间线新增远程开门 |
| 手动关门 | sensor-b | `{unlock=0}` | 继电器关闭 | 门锁状态回到关闭 |
| 触发门铃 | sensor-b | `{buzz=500,rgb=[0,0,255]}` | 蜂鸣器短响，蓝灯 | 控制状态显示平台已接收 |
| 关闭蜂鸣器 | sensor-b | `{buzz=0}` | 蜂鸣器关闭 | 诊断日志显示 buzz=0 回包 |
| 触发告警 | sensor-b | `{alert=2}` | 红灯，蜂鸣器长响 | 安全模式显示报警 |
| 解除告警 | sensor-b | `{reset=1}` | 蜂鸣器关闭，绿灯 | 时间线新增告警解除 |
| 启用夜间模式 | sensor-c | `{night=1}` | sensor-c 进入夜间判断 | 诊断日志显示 night=1 |
| 白天模式 | sensor-c | `{night=0}` | sensor-c 退出强制夜间 | 诊断日志显示 night=0 |

WebSocket 下发格式：

```json
{"method":"control","addr":"节点MAC地址","data":"{unlock=1}"}
```

节点 MAC：

| 节点 | MAC |
| --- | --- |
| sensor-a | `00:12:4B:00:1C:45:BD:01` |
| sensor-b | `00:12:4B:00:1C:45:BB:54` |
| sensor-c | `00:12:4B:00:1C:46:65:DE` |

## 6. 建议演示流程

1. 启动 Python 上位机，确认右上角显示“智云已连接”。
2. 等待 sensor-a 上报，环境面板显示温度、湿度、光照。
3. 在门口触发 PIR，访客状态显示有人靠近，事件时间线出现 PIR 检测。
4. 按门铃/触摸，门铃状态变为响铃，时间线出现门铃事件。
5. 点击“远程开门”，sensor-b 继电器动作，时间线出现远程开门。
6. 点击“触发告警”，红灯/蜂鸣器动作，页面安全模式显示报警。
7. 点击“解除告警”，蜂鸣器关闭，页面恢复安全。
8. 点击“日统计/事件统计”，展示 MySQL 统计图。
9. 点击“导出日志/导出环境”，生成 CSV。

## 7. 当前检查结论

- sensor-a：已实现环境采集和上报，满足要求。
- sensor-b：已实现门锁、蜂鸣器、RGB、告警/复位控制，满足要求。
- sensor-c：已实现 PIR、门磁、门铃、徘徊、夜间告警逻辑，满足要求。
- sensor-e：已实现智云 WebSocket、PyQt 页面、MySQL、统计、导出，满足 Python 端要求。
- C 端已修正普通夜间状态误判为安防事件的问题。

